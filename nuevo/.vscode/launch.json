{
    "configurations": [
    {
        "name": "(gdb) Adjuntar",
        "type": "cppdbg",
        "request": "attach",
        "program": "${fileDirname}/bin/${fileBasenameNoExtension}",
        "processId": "${command:pickProcess}",
        "MIMode": "lldb"
    }
    ]
}